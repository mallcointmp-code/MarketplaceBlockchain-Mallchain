// Lightweight mock of @cosmjs/stargate SigningStargateClient for unit tests
class SigningStargateClient {
  static async connectWithSigner(rpcEndpoint, signer, opts) {
    return new SigningStargateClient(rpcEndpoint, signer, opts)
  }

  constructor(rpcEndpoint, signer, opts) {
    this.rpcEndpoint = rpcEndpoint
    this.signer = signer
    this.registry = opts && opts.registry
  }

  async signAndBroadcast(sender, msgs, fee, memo) {
    // Return a deterministic fake response for tests
    return { code: 0, transactionHash: '0xdeadbeef', rawLog: '[]' }
  }
}

module.exports = { SigningStargateClient }
